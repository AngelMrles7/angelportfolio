import React from "react";
import Slider from "../../ui/slider/Slider";

const Projects = () => {
  return (
    <section className="section-projects">
      <h2 className="section-projects__title">Proyects</h2>
      <Slider />
    </section>
  );
};

export default Projects;
